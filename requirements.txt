## T_mem · Python dependencies
##
## 安装:
##     pip install -r requirements.txt
##
## 注意:
##   - venus_api_base (腾讯 Venus LLM SDK) 是内部依赖，不在 PyPI 上。
##     需要从公司内部源 `pip install venus_api_base` 或本地 wheel 安装,
##     这个依赖被 T_mem/llm/venus_provider.py 使用。
##   - 如果只通过 OpenAI-compatible endpoint 调用 LLM / Embedding / Reranker,
##     可以跳过 venus_api_base。
##   - json_repair 为可选依赖，用于 LLM JSON 输出解析容错；不安装也不影响运行。

## ---- core runtime ----
openai>=1.30.0
pydantic>=2.5.0
python-dotenv>=1.0.0
requests>=2.31.0
aiohttp>=3.9.0
urllib3>=2.0.0
rich>=13.7.0

## ---- numerical / data ----
numpy>=1.24.0

## ---- text / embedding / retrieval ----
nltk>=3.8.1
rank_bm25>=0.2.2

## ---- optional: JSON repair (LLM输出容错) ----
json_repair>=0.25.0
