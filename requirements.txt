## T_mem · Python dependencies
##
## Install:
##     pip install -r requirements.txt
##
## Notes:
##   - All external services (LLM / Embedding / Reranker) are accessed through
##     OpenAI-compatible endpoints; no provider-specific SDK is required.
##   - json_repair is optional and used for LLM JSON-output tolerance.

## ---- core runtime ----
openai>=1.30.0
pydantic>=2.5.0
python-dotenv>=1.0.0
requests>=2.31.0
aiohttp>=3.9.0
urllib3>=2.0.0
rich>=13.7.0

## ---- numerical / data ----
numpy>=1.24.0

## ---- text / embedding / retrieval ----
nltk>=3.8.1
rank_bm25>=0.2.2

## ---- optional: JSON repair (LLM output tolerance) ----
json_repair>=0.25.0
